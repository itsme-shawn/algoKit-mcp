# Product Requirements Document (PRD)
# BOJ 학습 도우미 MCP Server

**버전**: 2.0 (Keyless Architecture)
**작성일**: 2026-02-13
**마지막 업데이트**: 2026-02-13
**상태**: Phase 3 완료 (Keyless 아키텍처 구현)

---

## 1. 프로젝트 개요

### 1.1 제품 비전

BOJ 학습 도우미는 **백준 온라인 저지(Baekjoon Online Judge) 알고리즘 문제를 효과적으로 학습**할 수 있도록 돕는 AI 기반 MCP(Model Context Protocol) 서버입니다.

사용자가 Claude와 자연스럽게 대화하며 문제를 검색하고, 적절한 난이도의 힌트를 받고, 해결 후 체계적인 복습 문서를 생성할 수 있는 통합 학습 환경을 제공합니다.

### 1.2 한 줄 정의

**백준 ID 기반으로 풀이 이력을 분석하고, 학습 리포트와 문제별 복기 피드백 파일을 자동 생성하는 개인 알고리즘 학습 분석 서버**

### 1.3 문제 정의

**현재 BOJ 학습의 문제점**:

1. **전략 없는 문제 풀이**
   - 체계적인 학습 계획 없이 무작위로 문제 선택
   - 현재 수준에 맞지 않는 문제로 시간 낭비

2. **취약 알고리즘 인지 불가**
   - 어떤 알고리즘이 약한지 정량적으로 파악하기 어려움
   - 같은 유형의 문제를 반복적으로 틀림

3. **티어 정체 구간 돌파 전략 부재**
   - Silver/Gold 구간에서 장기간 정체
   - 다음 단계로 올라가기 위한 명확한 학습 방향성 부족

4. **복기 기록 자동화 없음**
   - 해결한 문제를 다시 보지 않음
   - 풀이 과정에서 배운 점을 기록하지 않음

5. **문제별 개선점 정리 안 함**
   - 왜 틀렸는지, 어떻게 개선할 수 있는지 분석 부재
   - 비슷한 실수를 반복

**결과적으로**:
- 많이 풀어도 실력이 체계적으로 상승하지 않음
- Gold 진입에서 정체
- 학습 효율성 저하

**타겟 사용자**:
- 알고리즘 문제 해결 능력을 향상시키고자 하는 학생 및 취업 준비생
- 코딩 테스트를 준비하는 개발자
- BOJ를 통해 알고리즘을 체계적으로 학습하고자 하는 모든 사용자
- 특히 **티어 정체 구간에서 돌파구를 찾는 학습자**

### 1.4 제품 목표

**🎯 Primary Goal (필수 기능)**:
1. **백준 ID 기반 전체 학습 분석 리포트 자동 생성**
   - 풀이 이력 종합 분석
   - 취약 알고리즘 정량화
   - 티어 정체 구간 탐지
   - 맞춤 학습 전략 제안

2. **특정 문제 풀이 후 복기 파일 자동 생성**
   - 문제별 핵심 개념 정리
   - 풀이 전략 비교
   - 개선 포인트 제시
   - 배운 점 정리 섹션

**🎯 Secondary Goal (부가 기능)**:
- 맞춤 문제 추천 시스템
- 단계별 힌트 제공
- 학습 진척도 시각화
- 알고리즘별 취약도 점수

**비목표 (Out of Scope for MVP)**:
- 실시간 코드 실행 및 채점
- 사용자 계정 및 로그인 시스템
- 문제 풀이 영상이나 강의 콘텐츠
- 소셜 기능 (친구, 그룹 스터디 등)

---

## 2. 핵심 기능 명세

### 2.1 문제 검색 및 발견 (Problem Search & Discovery)

#### 기능 1: `search_problems` - 문제 검색
**설명**: 다양한 필터를 사용하여 BOJ 문제를 검색합니다.

**기능 요구사항**:
- 키워드로 문제 검색 (제목, 번호, 태그)
- 난이도 범위 필터링 (Bronze ~ Ruby, 레벨 1-30)
- 알고리즘 태그 필터링 (DP, 그리디, 그래프 등)
- 정렬 옵션 (난이도, 문제 번호, 평균 시도 횟수)
- 페이지네이션 지원

**입력**:
```typescript
{
  query?: string;           // "이분 탐색", "1000", "투 포인터" 등
  level_min?: number;       // 1-30 (예: 11 = Gold V)
  level_max?: number;       // 1-30 (예: 15 = Gold I)
  tag?: string;             // "dp", "greedy", "graphs" 등
  sort?: "level" | "id" | "average_try";
  direction?: "asc" | "desc";
  page?: number;
}
```

**출력**:
```typescript
{
  count: number;            // 총 결과 개수
  problems: [
    {
      problemId: number;
      title: string;        // 한글 제목
      level: number;        // 1-30
      tierName: string;     // "Gold III"
      tags: string[];       // ["dp", "greedy"]
      acceptedCount: number;
      averageTries: number;
    }
  ]
}
```

**사용 시나리오**:
- "Gold 티어의 DP 문제를 찾고 싶어"
- "이분 탐색 문제 중에서 쉬운 것부터 보여줘"
- "평균 시도 횟수가 적은 Silver 문제 추천해줘"

#### 기능 2: `get_problem` - 문제 상세 정보
**설명**: 특정 문제의 상세 정보를 조회합니다.

**기능 요구사항**:
- 문제 ID로 메타데이터 조회
- BOJ 문제 링크 제공
- 통계 정보 (해결한 사용자 수, 평균 시도)
- 알고리즘 태그 목록

**입력**:
```typescript
{
  problem_id: number;  // 예: 1000
}
```

**출력**:
```typescript
{
  problemId: number;
  titleKo: string;
  titleEn: string;
  level: number;
  tierName: string;
  tags: Array<{
    key: string;
    displayName: string;
  }>;
  acceptedUserCount: number;
  averageTries: number;
  bojLink: string;  // https://www.acmicpc.net/problem/1000
}
```

**사용 시나리오**:
- "1927번 문제에 대해 알려줘"
- "이 문제의 난이도와 태그가 뭐야?"

#### 기능 3: `search_tags` - 알고리즘 태그 검색
**설명**: 알고리즘 태그를 키워드로 검색합니다.

**기능 요구사항**:
- 태그 키워드 검색
- 다국어 표시명 제공
- 각 태그의 문제 수 표시

**입력**:
```typescript
{
  query: string;  // "dynamic", "그래프" 등
}
```

**출력**:
```typescript
{
  tags: Array<{
    key: string;              // "dp"
    displayNameKo: string;    // "다이나믹 프로그래밍"
    displayNameEn: string;    // "Dynamic Programming"
    problemCount: number;     // 2847
  }>
}
```

**사용 시나리오**:
- "다이나믹 프로그래밍 관련 태그 검색해줘"
- "그래프 알고리즘에는 어떤 태그들이 있어?"

---

### 2.2 학습 지원 (Learning Support)

#### 기능 4: `analyze_problem` - 문제 분석 및 구조화된 힌트 데이터 제공 (Keyless)
**설명**: 문제를 분석하여 구조화된 힌트 데이터를 JSON으로 제공합니다. Claude Code가 이를 자연어로 변환합니다.

**Keyless 아키텍처**:
- **MCP 서버**: 결정적 JSON 데이터만 반환
- **Claude Code**: 자연어 생성 담당
- **Zero Configuration**: API 키 불필요

**기능 요구사항**:
- 3단계 힌트 포인트 시스템
  - **레벨 1 (Pattern)**: 알고리즘 패턴 인식 (key + detail)
  - **레벨 2 (Insight)**: 핵심 통찰 (key + detail + example)
  - **레벨 3 (Strategy)**: 구현 전략 (key + steps[])
- 정적 힌트 패턴 매핑 (`HINT_PATTERNS`)
- 알고리즘별 사전 정의된 힌트 (dp, greedy, graphs, math 등)
- 난이도 컨텍스트 (티어, 백분위, 이모지)
- 제약사항 및 주의사항 자동 생성
- 유사 문제 추천 (±2 티어 범위)

**입력**:
```typescript
{
  problem_id: number;
  include_similar?: boolean;  // 유사 문제 추천 포함 (기본값: true)
}
```

**출력** (JSON):
```typescript
{
  problem: {
    problemId: number;
    titleKo: string;
    level: number;
    tags: Tag[];
    // ... 문제 메타데이터
  },
  difficulty: {
    tier: string;           // "Silver II"
    level: number;          // 9
    emoji: string;          // "🟡"
    percentile: string;     // "초급 (상위 70-80%)"
    context: string;        // "Silver 난이도의 DP 문제"
  },
  algorithm: {
    primary_tags: string[];              // ["다이나믹 프로그래밍", "이분 탐색"]
    tag_explanations: Record<string, string>;  // 태그별 설명
    typical_approaches: string[];         // ["Bottom-up DP", "Top-down DP"]
    time_complexity_typical: string;      // "O(N) ~ O(N²)"
    space_complexity_typical: string;     // "O(N) ~ O(N²)"
  },
  hint_points: [
    {
      level: 1,
      type: "pattern",
      key: "동적 프로그래밍",
      detail: "DP의 전형적인 최적 부분 구조를 가진 문제입니다..."
    },
    {
      level: 2,
      type: "insight",
      key: "상태 정의와 점화식",
      detail: "dp[i]의 의미를 명확히 정의하고...",
      example: "dp[i] = min(dp[i-1], dp[i/2], dp[i/3]) + 1"
    },
    {
      level: 3,
      type: "strategy",
      key: "Bottom-up 또는 Top-down 구현",
      steps: [
        "1. 상태 정의 (dp[i]의 의미)",
        "2. 초기값 설정 (base case)",
        "3. 점화식 구현",
        "4. 계산 순서 결정",
        "5. 최종 답 반환"
      ]
    }
  ],
  constraints: [...],
  gotchas: [...],
  similar_problems: [...]
}
```

**힌트 생성 전략**:

**레벨 1 예시** (문제: 최단 경로 찾기):
```
이 문제는 그래프 탐색 문제입니다.
두 지점 사이의 최단 경로를 찾아야 하는데,
가중치가 없는 그래프에서 사용할 수 있는
기본적인 탐색 알고리즘을 생각해보세요.
```

**레벨 2 예시**:
```
BFS(너비 우선 탐색)를 사용하면 좋습니다.
BFS는 시작점에서 가까운 노드부터 차례로 탐색하므로,
처음 도착한 경로가 자동으로 최단 경로가 됩니다.
큐 자료구조를 사용하여 방문할 노드를 관리하세요.
```

**레벨 3 예시**:
```
알고리즘 단계:
1. 시작 노드를 큐에 넣고 방문 표시
2. 큐에서 노드를 꺼내고, 인접한 미방문 노드들을 큐에 추가
3. 각 노드까지의 거리를 기록 (이전 노드 거리 + 1)
4. 목표 노드에 도달할 때까지 2-3 반복
5. 목표 노드의 기록된 거리가 최단 거리

시간 복잡도: O(V + E) (V: 노드 수, E: 간선 수)
```

**사용 시나리오**:
- "이 문제 어떻게 접근해야 할지 막막해. 힌트 줘"
- "DP라는 건 알겠는데 상태 정의를 모르겠어. 더 자세한 힌트 필요해"

---

#### 기능 5: `generate_review_template` - 복습 템플릿 및 가이드 제공 (Keyless)
**설명**: 복습 문서 템플릿과 작성 가이드 프롬프트를 제공합니다. Claude Code가 대화형으로 복습 문서를 작성합니다.

**Keyless 아키텍처**:
- **MCP 서버**: 템플릿 + 가이드 프롬프트 반환
- **Claude Code**: 사용자와 대화하며 복습 문서 작성
- **Zero Configuration**: API 키 불필요

**기능 요구사항**:
- 마크다운 템플릿 생성 (문제 정보 자동 포함)
- 빈 섹션 제공 (풀이 접근법, 복잡도, 인사이트, 어려웠던 점)
- 관련 문제 자동 추천 (±2 티어 범위, 같은 태그)
- 작성 가이드 프롬프트 생성 (Claude Code가 사용)
- 해결 날짜 자동 추가

**입력**:
```typescript
{
  problem_id: number;
  user_notes?: string;  // (선택) 미리 작성한 메모
}
```

**출력** (JSON):
```json
{
  "template": "# [1927] 최소 힙\n\n**티어**: 🟡 Silver II\n**태그**: 자료 구조, 우선순위 큐\n**해결한 사람**: 73,425명\n**평균 시도**: 1.8회\n**문제 링크**: https://www.acmicpc.net/problem/1927\n**해결 날짜**: 2026-02-13\n\n---\n\n## 풀이 접근법\n\n(여기에 작성)\n\n## 시간/공간 복잡도\n\n- **시간 복잡도**: \n- **공간 복잡도**: \n\n## 핵심 인사이트\n\n(여기에 작성)\n\n## 어려웠던 점\n\n(여기에 작성)\n\n## 관련 문제\n\n- [11279] 최대 힙 (Silver II)\n- [11286] 절댓값 힙 (Silver I)\n- [1655] 가운데를 말해요 (Gold II)\n\n---\n\n*이 문서는 algokit를 사용하여 생성되었습니다.*\n",
  "problem_summary": {
    "problemId": 1927,
    "titleKo": "최소 힙",
    "tier": "Silver II",
    "tags": ["자료 구조", "우선순위 큐"],
    "acceptedUserCount": 73425,
    "averageTries": 1.8,
    "bojLink": "https://www.acmicpc.net/problem/1927"
  },
  "related_problems": [
    { "problemId": 11279, "titleKo": "최대 힙", "tier": "Silver II" },
    { "problemId": 11286, "titleKo": "절댓값 힙", "tier": "Silver I" },
    { "problemId": 1655, "titleKo": "가운데를 말해요", "tier": "Gold II" }
  ],
  "guide_prompt": "이 템플릿을 사용하여 복습 문서를 작성합니다. 사용자에게 다음을 질문하세요:\n1. 풀이 접근법: 어떤 알고리즘과 자료구조를 사용했나요?\n2. 시간/공간 복잡도: 계산해보셨나요?\n3. 핵심 인사이트: 이 문제에서 배운 핵심 개념은 무엇인가요?\n4. 어려웠던 점: 어떤 부분이 어려웠고, 어떻게 해결했나요?\n\n사용자 답변을 받으면 template의 해당 섹션에 채워넣고 최종 마크다운 문서를 반환하세요."
}
```

**Claude Code의 역할**:
1. `template`를 기본 틀로 사용
2. `guide_prompt`를 참고하여 사용자와 대화
3. 사용자 답변을 `template`에 채워넣기
4. 최종 완성된 마크다운 문서 반환

**사용 시나리오**:
- "방금 푼 1927번 문제 복습 문서 만들어줘"
- "내 풀이 접근법을 정리해서 나중에 다시 볼 수 있게 해줘"

---

### 2.3 학습 분석 (Learning Analytics) - 핵심 기능

#### 기능 6: `analyze_user` - 백준 ID 기반 전체 학습 리포트 생성

**설명**: 백준 사용자 ID를 기반으로 전체 풀이 이력을 분석하고, 취약 알고리즘, 티어 정체 구간, 맞춤 학습 전략을 포함한 종합 리포트를 자동 생성합니다.

**기능 요구사항**:
- solved.ac API를 통한 사용자 풀이 이력 수집
- 티어별 문제 분포 분석
- 알고리즘별 풀이 비율 및 취약도 계산
- 정체 구간 탐지 (같은 티어 75% 이상)
- 2주 맞춤 학습 전략 제안
- 추천 문제 리스트 자동 생성

**입력**:
```typescript
{
  boj_id: string;           // 백준 사용자 ID
  analysis_depth?: "basic" | "detailed";  // 기본값: "detailed"
  recent_count?: number;    // 최근 N개 문제 분석 (기본값: 50)
}
```

**출력 (파일 생성)**:
```
/reports/{boj_id}_learning_report.md
```

**리포트 구조**:
```markdown
# BOJ 학습 분석 리포트 - {boj_id}

## 📊 1. 현재 추정 실력
- **현재 티어**: Silver 2
- **상위 백분율**: 상위 30%
- **총 해결 문제 수**: 127문제

## 📈 2. 티어 분포
| 티어 | 문제 수 | 비율 |
|------|--------|------|
| Bronze | 15 | 12% |
| Silver | 95 | 75% |
| Gold | 17 | 13% |

**분석**: Silver 티어에 집중되어 있으며, Gold 진입 초기 단계

## 🧩 3. 알고리즘 분포
| 알고리즘 | 문제 수 | 비율 | 취약도 |
|---------|--------|------|--------|
| 그래프 | 36 | 28% | ⭐⭐ 보통 |
| 브루트포스 | 24 | 19% | ⭐ 양호 |
| 구현 | 18 | 14% | ⭐ 양호 |
| DP | 9 | 7% | ⭐⭐⭐ 취약 |
| 문자열 | 4 | 3% | ⭐⭐⭐⭐ 매우 취약 |

## ⚠️ 4. 취약 영역 상세 분석

### 4.1 동적 계획법 (DP)
- **평균 제출 횟수**: 4.2회 (전체 평균 2.1회)
- **정답률**: 45% (전체 평균 72%)
- **문제점**: 상태 정의와 점화식 도출에 어려움
- **개선 방향**:
  - 기본 DP 패턴 학습 (LIS, LCS, 배낭 문제)
  - 1차원 DP부터 체계적으로 연습

### 4.2 문자열
- **풀이 문제 수**: 4개 (매우 부족)
- **평균 소요 시간**: 길음
- **개선 방향**:
  - 기본 문자열 처리 문제부터 시작
  - KMP, Trie 등 고급 알고리즘 학습

## 🔄 5. 티어 정체 분석
**최근 40문제 중 30개가 Silver 3 (75%)**

**정체 패턴 감지**:
- Gold 문제 시도 시 정답률 급감 (20%)
- Silver 3-2 구간을 벗어나지 못함
- DP와 그리디 약점이 Gold 진입 장벽

**돌파 전략**:
1. 취약 알고리즘 집중 공략 (DP 우선)
2. Gold 5 쉬운 문제부터 도전
3. 실패한 문제 복기 필수

## 📚 6. 2주 학습 전략

### Week 1: DP 기본기 다지기
- [ ] DP 입문 문제 6개 (Silver 2-3)
  - [1463] 1로 만들기
  - [9095] 1, 2, 3 더하기
  - [2579] 계단 오르기
  - [1149] RGB거리
  - [11726] 2×n 타일링
  - [11053] 가장 긴 증가하는 부분 수열
- [ ] 풀이 후 반드시 복기 문서 작성
- [ ] DP 상태 전이 패턴 정리

### Week 2: Gold 진입 준비
- [ ] Gold 5 그래프 문제 4개
  - [7576] 토마토 (BFS)
  - [1697] 숨바꼭질 (BFS)
  - [2606] 바이러스 (DFS)
  - [11724] 연결 요소의 개수 (DFS)
- [ ] 문자열 기본 3문제
  - [9086] 문자열
  - [11720] 숫자의 합
  - [10809] 알파벳 찾기

## 🎯 7. 맞춤 문제 추천

### 취약 알고리즘 보완
**DP (Silver 2-3 난이도)**:
1. [1463] 1로 만들기 - DP 입문의 정석
2. [9095] 1, 2, 3 더하기 - 기본 점화식 연습
3. [2579] 계단 오르기 - 조건이 있는 DP

**문자열 (Silver 4-5 난이도)**:
1. [10809] 알파벳 찾기 - 문자열 기본
2. [1157] 단어 공부 - 빈도수 카운팅
3. [2675] 문자열 반복 - 반복문 연습

### Gold 진입 준비
**Gold 5 난이도** (그래프 중심):
1. [7576] 토마토 - BFS 기본
2. [1697] 숨바꼭질 - BFS 응용
3. [2467] 용액 - 투 포인터

## 📝 8. 액션 아이템
- [ ] 주 3회 이상 꾸준히 문제 풀이
- [ ] DP 문제 풀이 시 상태 전이 다이어그램 그리기
- [ ] 틀린 문제는 반드시 다음날 다시 풀기
- [ ] 매주 일요일 주간 회고 작성

---
*생성 일시: 2026-02-13*
*분석 기준: 최근 50문제*
```

**데이터 수집 항목**:
1. **기본 프로필**
   - 현재 티어
   - 총 해결 문제 수
   - 랭킹

2. **문제 이력**
   - 푼 문제 목록 (ID, 티어, 태그)
   - 각 문제별 제출 횟수
   - 해결 날짜

3. **알고리즘 분포**
   - 태그별 문제 수
   - 태그별 평균 제출 횟수
   - 태그별 정답률

**취약도 계산식**:
```
취약도 점수 = (평균 제출 횟수 / 전체 평균) × (1 - 정답률) × 100
```

- 점수 80+ : ⭐⭐⭐⭐ 매우 취약
- 점수 60-80: ⭐⭐⭐ 취약
- 점수 40-60: ⭐⭐ 보통
- 점수 40- : ⭐ 양호

**정체 구간 탐지 로직**:
```typescript
// 최근 40문제 중 같은 티어가 75% 이상이면 정체로 판단
const recentProblems = problems.slice(0, 40);
const tierCounts = countByTier(recentProblems);
const maxTierRatio = Math.max(...Object.values(tierCounts)) / 40;

if (maxTierRatio >= 0.75) {
  // 정체 구간 감지
  // 돌파 전략 제시
}
```

**사용 시나리오**:
- `/analyze shawnhoon` - shawnhoon 사용자의 전체 학습 리포트 생성
- "내 백준 ID는 user123이야. 학습 분석 리포트 만들어줘"
- "최근 100문제 기준으로 상세 분석해줘"

**슬래시 커맨드 (향후)**:
- `/cote:analyze {boj_id}` - 전체 학습 분석

---

### 2.4 문제별 복기 강화 (Problem Review Enhancement)

`create_review` 기능을 확장하여 AI 기반 코드 피드백과 개선점을 자동 생성합니다.

**확장 기능**:
1. **코드 분석** (선택사항)
   - 사용자 코드 입력 시 자동 분석
   - 변수명, 로직, 자료구조 선택 피드백
   - 시간복잡도 자동 계산

2. **다른 풀이 비교**
   - 최적 풀이 접근법 제시
   - 여러 풀이 방식 비교

3. **복습 체크리스트 자동 생성**
   - 핵심 개념 이해도 체크
   - 비슷한 유형 연습 필요 여부

**강화된 출력 예시**:
```markdown
## ✨ 코드 피드백

### 개선 포인트
1. **변수명 명확성**
   - `arr` → `numbers` (더 명확한 의미)
   - `dp` → `minCost` (목적 반영)

2. **로직 단순화**
   ```python
   # 현재 코드
   for i in range(len(arr)):
       if arr[i] > target:
           continue

   # 개선 코드
   for num in arr:
       if num > target:
           continue
   ```

3. **자료구조 최적화**
   - 리스트 대신 deque 사용 권장
   - 탐색 시간 O(n) → O(1)

## 🔁 복습 체크리스트
- [ ] 이 알고리즘의 핵심 원리를 완전히 이해했는가?
- [ ] 비슷한 문제를 혼자 풀 수 있는가?
- [ ] 시간복잡도를 정확히 계산할 수 있는가?
- [ ] 다른 풀이 방법도 고민해봤는가?

## 🔗 다음 단계
이 문제를 풀었다면 다음 문제를 추천합니다:
1. [유사 난이도] {problem_id} - 같은 알고리즘, 비슷한 난이도
2. [한 단계 Up] {problem_id} - 같은 알고리즘, 더 어려운 난이도
3. [응용 문제] {problem_id} - 복합 알고리즘
```

---

## 3. 사용자 시나리오

### 3.1 시나리오 1: 문제 발견 및 학습 시작

**사용자**: "DP 연습하고 싶은데 Gold 티어 문제 추천해줘"

**시스템 동작**:
1. `search_problems` 호출
   - `tag`: "dp"
   - `level_min`: 11
   - `level_max`: 15
   - `sort`: "level"
   - `direction`: "asc"

2. 문제 목록 반환 (난이도 순)

3. 사용자에게 제시:
   ```
   Gold 티어의 DP 문제를 찾았습니다:

   1. [11053] 가장 긴 증가하는 부분 수열 (Gold II)
      - 태그: DP, 이분 탐색
      - 73,892명 해결, 평균 2.3회 시도

   2. [1149] RGB거리 (Gold V)
      - 태그: DP
      - 89,234명 해결, 평균 1.6회 시도

   어떤 문제부터 시작하시겠어요?
   ```

---

### 3.2 시나리오 2: 문제 풀이 중 막힘 → 힌트 요청

**사용자**: "11053번 문제 풀고 있는데 막혔어. 분석해줘"

**시스템 동작** (Keyless):
1. `analyze_problem` 호출
   - `problem_id`: 11053
   - `include_similar`: true

2. MCP 서버 응답 (JSON, < 500ms):
   - 문제 메타데이터
   - 난이도 컨텍스트
   - 알고리즘 정보
   - 3단계 힌트 포인트 (정적 패턴)
   - 제약사항, 주의사항
   - 유사 문제 추천

3. Claude Code가 JSON 파싱 후 자연어 변환:
   ```
   이 문제는 **동적 프로그래밍(DP)** 문제입니다.

   DP의 전형적인 최적 부분 구조를 가진 문제로,
   큰 문제를 작은 부분 문제로 나누어 해결할 수 있습니다.

   어떤 레벨의 힌트가 필요하신가요?
   - Level 1: 알고리즘 패턴 인식
   - Level 2: 핵심 통찰 (dp[i] 정의와 점화식)
   - Level 3: 구현 전략 (단계별 안내)
   ```

**사용자**: "레벨 2 힌트 줘"

**Claude Code 동작**:
1. 앞서 받은 JSON에서 `hint_points[1]` (Level 2) 추출
2. 자연어로 변환:
   ```
   **핵심 통찰: 상태 정의와 점화식**

   dp[i]의 의미를 명확히 정의하고, 이전 상태에서
   현재 상태로 전이하는 점화식을 세워야 합니다.

   예시: dp[i] = max(dp[j]) + 1 (단, j < i이고 arr[j] < arr[i])

   이제 이 점화식을 코드로 구현해보세요!
   ```

---

### 3.3 시나리오 3: 백준 ID 기반 전체 학습 분석

**사용자**: "/analyze shawnhoon"

**시스템 동작**:
1. `analyze_user` 호출
   - `boj_id`: "shawnhoon"
   - `analysis_depth`: "detailed"
   - `recent_count`: 50

2. solved.ac API를 통해 데이터 수집:
   - 사용자 프로필 (티어, 랭킹)
   - 해결한 문제 목록 (최근 50개)
   - 각 문제의 메타데이터 (티어, 태그, 통계)

3. 분석 수행:
   - 티어별 분포 계산
   - 알고리즘별 비율 및 취약도 점수 계산
   - 정체 구간 탐지 (최근 40문제 분석)
   - 맞춤 학습 전략 생성

4. 리포트 파일 생성:
   ```
   📄 shawnhoon_learning_report.md 파일이 생성되었습니다.

   주요 발견 사항:
   - 현재 Silver 2 티어 (상위 30%)
   - DP 알고리즘 취약 (⭐⭐⭐ 취약도 점수 65)
   - 최근 40문제 중 75%가 Silver 3 → 정체 구간 감지
   - 2주 학습 전략: DP 기본기 다지기 + Gold 진입 준비

   상세 내용은 리포트 파일을 확인하세요.
   ```

5. 사용자 액션:
   - 리포트 확인 후 취약 알고리즘 집중 학습
   - 추천 문제 리스트를 바탕으로 2주 계획 수립

---

### 3.4 시나리오 4: 문제 해결 후 복습 문서 작성

**사용자**: "1927번 힙 문제 풀었어. 복습 문서 만들어줘"

**시스템 동작** (Keyless):
1. `generate_review_template` 호출
   - `problem_id`: 1927
   - `user_notes`: (optional)

2. MCP 서버 응답 (JSON, < 500ms):
   ```json
   {
     "template": "# [1927] 최소 힙\n\n**티어**: 🟡 Silver II\n...\n## 풀이 접근법\n\n(여기에 작성)\n...",
     "problem_summary": { "problemId": 1927, "titleKo": "최소 힙", ... },
     "related_problems": [...],
     "guide_prompt": "이 템플릿을 사용하여 복습 문서를 작성합니다. 사용자에게 다음을 질문하세요..."
   }
   ```

3. Claude Code가 `guide_prompt`를 활용하여 대화형 작성:
   ```
   복습 문서를 작성해드리겠습니다. 몇 가지 질문드릴게요:

   1. 어떤 접근법으로 해결하셨나요?
   ```

4. 사용자 응답:
   ```
   Python의 heapq 모듈을 사용했어요.
   최소 힙으로 구현했고, 0이 입력되면 최솟값을 출력하고 제거했습니다.
   ```

5. Claude Code가 추가 질문 (시간/공간 복잡도, 인사이트, 어려웠던 점)

6. 최종 문서 생성:
   ```
   복습 문서를 생성했습니다!

   [완성된 마크다운 문서 표시]

   추천 파일명: reviews/1927_최소힙_2026-02-13.md

   관련 문제도 추천드립니다:
   - [11279] 최대 힙 (Silver II)
   - [11286] 절댓값 힙 (Silver I)
   ```

---

### 3.4 시나리오 4: 태그 탐색

**사용자**: "그래프 관련해서 어떤 알고리즘 태그들이 있어?"

**시스템 동작**:
1. `search_tags` 호출
   - `query`: "그래프"

2. 관련 태그 목록 반환:
   ```
   그래프 관련 알고리즘 태그를 찾았습니다:

   1. graphs (그래프 이론) - 3,247개 문제
   2. dfs (깊이 우선 탐색) - 1,892개 문제
   3. bfs (너비 우선 탐색) - 1,654개 문제
   4. dijkstra (데이크스트라) - 287개 문제
   5. graph_traversal (그래프 탐색) - 2,103개 문제

   이 중 어떤 태그로 문제를 검색해드릴까요?
   ```

---

## 4. 기술 요구사항

### 4.1 시스템 아키텍처

**컴포넌트 구성**:
```
┌─────────────────┐
│  Claude Client  │ (사용자 인터페이스)
└────────┬────────┘
         │ MCP Protocol
         │
┌────────▼────────┐
│   MCP Server    │
│  (index.ts)     │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
┌───▼───┐ ┌──▼────────┐
│ Tools │ │ Services  │
│       │ │           │
│ - search│ - hint    │
│ - get  │   generator│
│ - hint │ - review   │
│ - review│   generator│
└───┬───┘ └──┬────────┘
    │        │
    └────┬───┘
         │
    ┌────▼────────┐
    │ API Client  │
    │ (solved.ac) │
    └─────────────┘
```

### 4.2 데이터 흐름

1. **문제 검색 플로우**:
   ```
   사용자 → Claude → MCP Tool (search_problems)
                      ↓
                  API Client → solved.ac API
                      ↓
                  데이터 포맷팅
                      ↓
                  사용자 ← Claude
   ```

2. **힌트 생성 플로우**:
   ```
   사용자 → Claude → MCP Tool (get_hint)
                      ↓
                  문제 메타데이터 조회 (API)
                      ↓
                  Hint Generator Service
                      ↓
                  LLM (Claude API) - 프롬프트 기반 힌트 생성
                      ↓
                  사용자 ← Claude
   ```

3. **복습 생성 플로우**:
   ```
   사용자 → Claude → MCP Tool (create_review)
                      ↓
                  문제 메타데이터 조회
                  + 사용자 입력
                      ↓
                  Review Generator Service
                      ↓
                  마크다운 템플릿 채우기
                      ↓
                  사용자 ← Claude (마크다운 문서)
   ```

### 4.3 기술 스택

**핵심 기술**:
- **Runtime**: Node.js (ES2022)
- **언어**: TypeScript 5.9.3 (strict mode)
- **MCP SDK**: `@modelcontextprotocol/sdk` v1.26.0
- **Validation**: Zod v4.3.6 (스키마 검증)

**외부 API**:
- **solved.ac API**: 문제 메타데이터 조회
  - Base URL: `https://solved.ac/api/v3`
  - 인증 불필요
  - CORS 제한 (서버 전용)
- ~~**Claude API**~~: **Keyless 아키텍처로 제거됨** (Zero Configuration)

**개발 도구**:
- **테스트**: vitest v4.0.18
- **TypeScript 실행**: tsx v4.21.0
- **빌드**: TypeScript 컴파일러

### 4.4 성능 요구사항

| 기능 | 목표 응답 시간 | 최대 응답 시간 |
|------|---------------|---------------|
| 문제 검색 | < 1초 | < 2초 |
| 문제 상세 조회 | < 0.5초 | < 1초 |
| 태그 검색 | < 0.5초 | < 1초 |
| 힌트 생성 | < 3초 | < 5초 |
| 복습 생성 | < 1초 | < 2초 |

**최적화 전략**:
- solved.ac API 응답 캐싱 (TTL: 1시간)
- 자주 조회되는 문제 메타데이터 우선 캐싱
- API 호출 실패 시 재시도 로직 (최대 3회)

### 4.5 보안 및 안정성

**에러 처리**:
- API 호출 실패 시 명확한 에러 메시지 반환
- 유효하지 않은 입력에 대한 검증 (Zod 스키마)
- 네트워크 타임아웃 설정 (10초)
- 사용자에게 친화적인 에러 메시지 (한글)

**API 사용 제한**:
- solved.ac API 요청 간 최소 100ms 간격 (rate limiting)
- 동시 요청 수 제한 (최대 5개)
- 캐싱으로 중복 요청 방지

**데이터 보안**:
- 사용자 입력 sanitization
- API 키 환경 변수 관리 (Claude API)
- 민감 정보 로깅 금지

---

## 5. 성공 지표 (Success Metrics)

### 5.1 기능적 지표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| 도구 정상 작동률 | 100% | 5개 도구 모두 테스트 통과 |
| API 통합 안정성 | 99% 이상 | API 호출 성공률 |
| 테스트 커버리지 | 80% 이상 | vitest 커버리지 리포트 |
| 치명적 버그 | 0건 | 프로덕션 배포 전 |

### 5.2 성능 지표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| 평균 검색 응답 시간 | < 1.5초 | 성능 테스트 |
| 힌트 생성 시간 | < 4초 | 성능 테스트 |
| 캐시 히트율 | > 70% | 로깅 분석 |
| API 호출 감소율 | > 60% | 캐싱 효과 측정 |

### 5.3 사용자 경험 지표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| 힌트 유용성 | 만족도 4/5 이상 | 사용자 피드백 |
| 복습 문서 활용도 | 70% 이상 사용 | 사용 패턴 분석 |
| 에러 메시지 명확성 | 이해도 90% 이상 | 사용자 피드백 |
| 워크플로우 직관성 | 추가 설명 불필요 | 사용성 테스트 |

---

## 6. 개발 로드맵

### Phase 1: MVP (1-2주) ✅ 완료
**목표**: 기본 문제 검색 및 조회 기능

- [x] 프로젝트 구조 설정
- [x] solved.ac API 클라이언트 구현
- [x] `search_problems` 도구 구현
- [x] `get_problem` 도구 구현
- [x] 기본 테스트 작성

**완료 기준**: Claude를 통해 BOJ 문제를 검색하고 상세 정보를 조회 가능 ✅

### Phase 2: 학습 지원 기능 (2주) ✅ 완료
**목표**: 태그 검색 기능

- [x] `search_tags` 도구 구현
- [x] 캐싱 메커니즘 추가
- [x] 통합 테스트 작성

**완료 기준**: 알고리즘 태그 검색 가능 ✅

### Phase 3: Keyless 힌트 및 복습 시스템 (3주) ✅ 완료
**목표**: API 키 없이 사용 가능한 학습 지원

**주요 설계 변경**:
- ❌ Claude API 통합 제거 → ✅ Keyless 아키텍처 도입
- ❌ `get_hint` (LLM 기반) → ✅ `analyze_problem` (정적 패턴)
- ❌ `create_review` (LLM 생성) → ✅ `generate_review_template` (템플릿 + 프롬프트)

**완료 항목**:
- [x] Keyless 아키텍처 설계
- [x] ProblemAnalyzer Service 구현 (590 lines)
- [x] ReviewTemplateGenerator Service 구현 (242 lines)
- [x] `analyze_problem` 도구 구현
- [x] `generate_review_template` 도구 구현
- [x] 3단계 힌트 포인트 데이터 구조 설계
- [x] 정적 힌트 패턴 매핑 (30개 알고리즘)
- [x] 관련 문제 추천 로직
- [x] 단위 및 통합 테스트 작성
- [x] 아키텍처 문서 작성

**완료 기준**:
- ✅ API 키 없이 문제 분석 및 복습 템플릿 생성 가능
- ✅ 응답 시간 < 500ms
- ✅ 결정적 출력 (테스트 안정성 향상)
- ✅ Claude Code와의 자연스러운 통합

### Phase 4: 완성도 & 최적화 (5주)
**목표**: 프로덕션 준비

- [ ] 성능 최적화 (캐싱, 병렬 처리)
- [ ] 에러 처리 강화
- [ ] 로깅 및 모니터링
- [ ] 전체 문서 검토 및 개선
- [ ] 베타 테스트 및 피드백 반영

**완료 기준**: 프로덕션 배포 가능한 안정성 및 문서화

---

## 7. 리스크 및 완화 전략

### 7.1 기술적 리스크

| 리스크 | 영향도 | 가능성 | 완화 전략 |
|--------|--------|--------|----------|
| solved.ac API 다운타임 | 높음 | 낮음 | 캐싱 강화, 대체 데이터 소스 준비 |
| Claude API 비용 초과 | 중간 | 중간 | 프롬프트 최적화, 토큰 사용량 모니터링 |
| 힌트 품질 불안정 | 높음 | 중간 | 프롬프트 템플릿 개선, 사용자 피드백 수집 |
| 성능 저하 (많은 요청) | 중간 | 중간 | Rate limiting, 캐싱, 비동기 처리 |

### 7.2 제품 리스크

| 리스크 | 영향도 | 가능성 | 완화 전략 |
|--------|--------|--------|----------|
| 힌트가 너무 명확함 | 중간 | 높음 | 단계별 힌트 레벨 세밀 조정 |
| 복습 문서 형식 불만족 | 낮음 | 중간 | 커스터마이징 옵션 추가 |
| 학습 효과 미미 | 높음 | 낮음 | 사용자 피드백 기반 개선, 메트릭 추적 |

---

## 8. 향후 확장 계획 (Post-MVP)

### 8.1 개인화 기능
- 사용자별 해결 기록 추적
- 약점 알고리즘 분석 및 추천
- 맞춤형 학습 계획 생성

### 8.2 협업 기능
- 스터디 그룹 지원
- 풀이 공유 및 토론
- 또래 진행도 비교

### 8.3 고급 분석
- 제출 코드 분석 및 최적화 제안
- 시간/공간 복잡도 자동 분석
- 최적 솔루션과 비교

### 8.4 학습 강화
- 간격 반복(Spaced Repetition) 시스템
- 문제 재풀이 스케줄링
- 기억 유지도 추적

---

## 9. 부록

### 9.1 티어 시스템 참고표

| 레벨 범위 | 티어 | 색상 | 대상 |
|----------|------|------|------|
| 1-5 | Bronze V-I | 🟤 갈색 | 프로그래밍 입문자 |
| 6-10 | Silver V-I | ⚪ 은색 | 기본 알고리즘 학습자 |
| 11-15 | Gold V-I | 🟡 금색 | 중급 알고리즘 실력자 |
| 16-20 | Platinum V-I | 🔵 청록색 | 고급 알고리즘 마스터 |
| 21-25 | Diamond V-I | 💎 청색 | 알고리즘 전문가 |
| 26-30 | Ruby V-I | 🔴 적색 | 최상위 마스터 |

### 9.2 주요 알고리즘 태그

| 태그 키 | 한글명 | 영문명 | 난이도 |
|---------|--------|--------|--------|
| dp | 다이나믹 프로그래밍 | Dynamic Programming | 중~상 |
| greedy | 그리디 | Greedy | 중 |
| graphs | 그래프 이론 | Graph Theory | 중~상 |
| dfs | 깊이 우선 탐색 | Depth-First Search | 중 |
| bfs | 너비 우선 탐색 | Breadth-First Search | 중 |
| binary_search | 이분 탐색 | Binary Search | 하~중 |
| implementation | 구현 | Implementation | 하~중 |
| data_structures | 자료 구조 | Data Structures | 중 |

---

**문서 변경 이력**:
- v1.0 (2026-02-13): 초안 작성
